#include "main.h"

/**
 * main - fuction that prints new line
 * @argc: number of arguments
 * @argv: array of arguments
 * Return: zero
 */
int main(int argc, char *argv[])
{
	printf("%s\n", *argv);
	return (argc);
}
